{
  "name": "housemaster-camera-measurements",
  "version": "0.1.0",
  "private": true,
  "description": "HouseMaster PWA — Camera Measurements persistence scaffold (G1). Offline-first, IndexedDB via Dexie.js, TypeScript.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --import ./src/camera-measurements/tests/setup.ts --test src/camera-measurements/tests/referenceIdentity.test.ts src/camera-measurements/tests/appendOnlyFacts.test.ts src/camera-measurements/tests/versionedAppendOnly.test.ts src/camera-measurements/tests/schemaMigration.test.ts src/camera-measurements/tests/surveyAssignment.test.ts src/camera-measurements/tests/scopeItem.test.ts src/camera-measurements/tests/measurementSession.test.ts"
  },
  "dependencies": {
    "dexie": "^4.0.10"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.6.3"
  }
}
